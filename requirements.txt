# ezAGI core requirements — python >= 3.10
# optional extras live in pyproject.toml:
#   pip install "ezagi[anthropic]"  -> anthropic Claude provider
#   pip install "ezagi[learn]"      -> SimpleMind/coach (jax, optax, numpy, scikit-learn, joblib, pandas, matplotlib)
#   pip install "ezagi[dev]"        -> pytest
# Ollama (local daemon or Ollama Cloud) needs no pip package: native HTTP via webmind/ollama_handler.py

# interact with the OpenAI API
openai>=1.60,<2

# interact with groq API models
groq>=0.13

# interact with the together.ai API models
together>=1.3

# load API environment variables from a .env file
python-dotenv>=1.0

# for UI integration as fastapi (2.x migration is a separate effort — stay on 1.4.x API)
nicegui>=1.4.29,<2

# high performance asynchronous HTTP client/server (ollama streaming)
aiohttp>=3.9

# synchronous HTTP client (endpoint probes, downloads)
httpx>=0.27

# for fast JSON processing
ujson>=5.10

# system and process utilities (self_healing)
psutil>=6.0
